[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "zgrydn-basic-lbp"
version = "0.0.1"

authors = [
    {name="Ozgur Aydin"}
]

description = "Example Python package"

readme = "README.md"

requires-python = ">=3.9"

dependencies = []